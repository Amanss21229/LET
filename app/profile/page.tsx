"use client";import Nav from "@/components/Nav";import {useState} from "react";
export default function Profile(){const [m,setM]=useState("");const save=async(e:any)=>{e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),className:f.get("className"),phone:f.get("phone")})});setM(r.ok?"Profile saved successfully.":"Please login with Google first.")};return <><Nav/><main className="wrap" style={{maxWidth:650}}><h1>My Profile</h1><p className="muted">Complete your profile after Google sign-in. All fields are editable.</p><form className="card" onSubmit={save}><input className="input" name="name" placeholder="Your Name" required/><input className="input" name="className" placeholder="Class (e.g. 9)" required/><input className="input" name="phone" placeholder="Mobile Number" required/><button className="btn primary">Save Profile</button><p>{m}</p></form></main></>}
