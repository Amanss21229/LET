import { NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { cookies } from "next/headers";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){if((await cookies()).get("let_admin")?.value!=="true")return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await params;const {email}=await req.json();const u=await prisma.user.findUnique({where:{email}});if(!u)return NextResponse.json({error:"User not found"},{status:404});return NextResponse.json(await prisma.batchAccess.upsert({where:{userId_batchId:{userId:u.id,batchId:id}},update:{},create:{userId:u.id,batchId:id}}));}
export async function DELETE(req:Request,{params}:{params:Promise<{id:string}>}){if((await cookies()).get("let_admin")?.value!=="true")return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await params;const {email}=await req.json();const u=await prisma.user.findUnique({where:{email}});if(u)await prisma.batchAccess.deleteMany({where:{userId:u.id,batchId:id}});return NextResponse.json({ok:true});}
